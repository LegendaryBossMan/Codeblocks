#include <stdio.h>
#include <stdlib.h>
#include <math.h>// needed for sqrt() and pow()

int main()
{
    float sideA;
    float sideB;
    printf("Enter side a and b: ");
    scanf("%f %f", &sideA &sideB);

    return 0;
}
